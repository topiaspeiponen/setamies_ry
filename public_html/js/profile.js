'use strict';

const url = "http://localhost:3000";

const createUserListings = (user_id) => {

};

//TODO Get user_id from the logged in user (requires passport and authentication to be in working state)
createUserListings(user_id);